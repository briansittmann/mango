## 1. Prerequisite and dependencies

- [x] 1.1 Archive `add-savings-mutations` (`openspec archive add-savings-mutations`) so `savings-editing` exists as a main spec — verify `openspec list --specs` lists `savings-editing`
- [x] 1.2 Add `@supabase/ssr` (`npm install @supabase/ssr`) — verify `package.json` lists it and `npm ls @supabase/ssr` resolves without errors
- [x] 1.3 Add `NEXT_PUBLIC_SUPABASE_URL` and `NEXT_PUBLIC_SUPABASE_ANON_KEY` to `.env.local` from the Supabase project (proposal → *Manual steps* 7) — verify `grep -c SUPABASE .env.local` prints 2 and the file is ignored by git (`git check-ignore .env.local`)
- [x] 1.4 Confirm the prerequisite `fix-free-margin-envelope-budgets` is archived — verify `openspec list` no longer shows it, and `supabase/migrations/0018_presupuestos_periodo_ciclo.sql`, `getFreeMargin` and `BudgetRow` in `lib/data/budget.ts` exist

## 2. Supabase clients and session plumbing

- [x] 2.1 Create `lib/supabase/server.ts` with `supabaseServer()` (D3): `createServerClient` over `next/headers` cookies with `getAll`/`setAll`, `setAll` tolerant of the Server Component case, `import 'server-only'`, env read inside the function — verify `npx tsc --noEmit` passes
- [x] 2.2 Create root `proxy.ts` (D4): refresh the session with `getClaims()`, redirect `/dashboard*` to `/login` without a user, matcher `['/dashboard/:path*', '/login', '/auth/:path*']` — verify `npm run build` reports the proxy and `curl -sI http://localhost:3000/dashboard` returns a 307 to `/login` with no cookies

## 3. Migration 0017

- [x] 3.1 Write `supabase/migrations/0017_funciones_dashboard.sql` with `copiar_presupuestos_ciclo`, `crear_categoria`, `actualizar_categoria`, `eliminar_categoria`, `reordenar_categorias`, `crear_movimiento_recurrente`, `actualizar_movimiento_recurrente` and `eliminar_movimiento_recurrente` (D12). Throughout: `security invoker`, explicit `p_usuario_id`, stable raise messages, the claim rule, `ciclo_mes` via `rango_ciclo_usuario`, and `repeticiones_insertadas = 1` on a claimed row. Budgets:
  - `copiar_presupuestos_ciclo` computes the current cycle itself and copies markers too
  - `crear_categoria` and `actualizar_categoria` call it before writing the current cycle's row
  - a null budget becomes a marker

  Verify by static review against the constraints in 0004–0016 and 0018 (unique names, FK `restrict` on `categoria_id`, check constraints on `tipo`/`categoria_id`, `unique (usuario_id, categoria_id, periodo)`, nullable `monto`), and that no function can write a `periodo` other than the current cycle's first day.
- [x] 3.2 After 1.4, apply 0001–0018 in order to the real project (proposal → *Manual steps* 1) — verify each file runs without error in the SQL Editor; record here which file fails, if any, and stop until it is fixed
  - 2026-09-24: 0001–0016 and 0018 applied without error. Before 0018, Brian's two `presupuestos` rows were moved from `periodo = 'mensual'` to `2026-08-26` (left by the old 0012). 0017 is applied after 3.1.
- [x] 3.3 Smoke-test the functions as `postgres` in the SQL Editor on Brian's seeded user. Verify the three raises, that the successful create shows up in `categorias`, and the copy results:
  - `crear_categoria` twice with the same name (the second raises `duplicate-category-name`)
  - `reordenar_categorias` with a missing id (raises `invalid-order`)
  - `eliminar_categoria` on a category with rows and a null target (raises `category-not-empty`)
  - after deleting the current cycle's `presupuestos` rows, `copiar_presupuestos_ciclo` twice: the first call restores them from the latest earlier cycle, the second adds nothing
  - 2026-09-24: 0017 applied through the MCP. Smoke run in one `do` block aborted by a final `raise` (nothing committed; Brian's 11 categories and 2 budgets unchanged afterwards). Results: create → row at `orden` 11; " SMOKE " → `duplicate-category-name`; missing id and repeated id → `invalid-order`; valid reversed order written; "vivienda" (one definition) with null target → `category-not-empty`; with a fake `2026-07-26` cycle (comida 400, ocio marker), first copy → 2 rows (400 + marker), second copy → still 2; clearing comida → marker in `2026-08-26`, `2026-07-26` kept; create with budget 75 → one row, `2026-08-26` only.
- [x] 3.4 Write and apply `supabase/migrations/0019_referencias_mismo_usuario.sql` (added during 8.3): composite FKs `(categoria_id, usuario_id)` on `transacciones`, `movimientos_recurrentes` and `presupuestos`, and `(movimiento_recurrente_id, usuario_id)` on `transacciones`, over new `unique (id, usuario_id)` keys, keeping `restrict` / `cascade` / `set null (movimiento_recurrente_id)` — verify a reference to another user's category or definition is rejected by the database
  - 2026-09-24: 8.3 found that `createExpense` with Brian's category id inserted a row of the test user pointing at it (RLS `with check` only looks at `usuario_id`; the RPCs of 0017 already checked ownership, the direct insert did not). Dry run in an aborted `do` block, then applied through the MCP: foreign category on a transaction or a budget and a foreign definition link → `foreign_key_violation`; own insert OK; `eliminar_movimiento_recurrente` unlinks and soft-deletes its charge; `eliminar_categoria` with and without budgets still works. The server action now rejects; the stray row was deleted and Brian's rows were unchanged.

## 4. Data layer (`lib/data/supabase/`)

- [x] 4.1 Create `context.ts` (`DataContext`) and `cycle.ts` (`cycleRange` via `rpc('rango_ciclo')`, `refInstantForMonth`, `shiftMonth`, `parseMonthParam` with the clamp to the cycle in progress, `localDateOf`, `cycleMonthOf`, D9) — verify `tsc` and, in a throwaway `node` script against the real project, that month `2026-09` with start day 26 and Europe/Dublin yields start `2026-08-26`, end `2026-09-25`, and that a month after the cycle in progress resolves to it
- [x] 4.2 Create `user.ts` with `findCurrentUsuario(client)` (D6) — verify `tsc`
- [x] 4.3 Create `dashboard.ts` with `resumenMensual(client, usuario, month?)` (D8): the four queries, bucketing by the six ranges, groups for every category, `fixed` with the D11 `isCharged` rule and `day ?? 1`, the copy RPC before reading budgets of the cycle in progress, budgets from the shown cycle's rows (marker = none), bars on spending without recurring charges, income entries with `recurring`, savings cycle/accumulated/target/history, `history`, `freeMargin` from `getFreeMargin`, `definitions` — verify `tsc` and that the assembled object for the test user (task 6.2) matches `dashboard-data` → *Seeded cycle*: 2 820 / 1 700 / 146 / 864, "transporte" 130 of 100, accumulated 2 646, six history points
  - 2026-09-24: verified through the real `/dashboard` for the seeded user (see 8.2). Bars include recurring charges, as the design and spec say ("transporte" 130 of 100); the task text above predates that and is stale.
- [x] 4.4 Create `expenses.ts`, `income.ts`, `savings.ts` factories (D13: `.select()` on every write, empty result → reject, `T12:00:00Z` dates, `moneda` from context, negative `monto` for withdrawals) — verify `tsc`
- [x] 4.5 Create `categories.ts` (RPCs of 3.1, `duplicate-category-name` → `DUPLICATE_CATEGORY_NAME`) and `recurring.ts` (RPCs plus `stop` as a plain update) — verify `tsc`
- [x] 4.6 Guard the demo: — verify `grep -rn "lib/supabase/server\|lib/data/supabase" app/demo lib/demo components` prints nothing

## 5. Routes and strings

- [x] 5.1 Add the `inicio` and `acceso` namespaces to `messages/es.json` and `messages/en.json` (D16) and set `app/layout.tsx` metadata title to "Mango" — verify `tsc` and `npm run lint`
- [x] 5.2 Rewrite `app/page.tsx` as Home: two links, "Demo" → `/demo` and "Entrar" → `/login`, texts from `inicio` — verify the page renders the two links in Spanish and "Log in" in English (locale cookie) and that `/` makes no request to a Supabase host
- [x] 5.3 Create `app/login/actions.ts` (`sendMagicLink`, `shouldCreateUser: false`, same `sent` result for success and "signups not allowed", `error` otherwise) and `app/login/page.tsx` (signed-in → `redirect('/dashboard')`; otherwise `LoginForm` with `useActionState`, e-mail input `type="email" required`, sent/error messages, `?error=enlace` message) — verify `tsc` and that `/login` renders the form with placeholder env values
- [x] 5.4 Create `app/auth/confirm/route.ts` (`verifyOtp({ token_hash, type })`, redirect to `/dashboard`, errors to `/login?error=enlace`) — verify `tsc` and that `GET /auth/confirm` without params redirects to `/login?error=enlace`
- [x] 5.5 Create `app/dashboard/actions.ts` (`'use server'`): one action per contract operation building the `DataContext` from `findCurrentUsuario`, calling the factories of section 4 and `revalidatePath('/dashboard')`; `signOut` (`auth.signOut()` + `redirect('/')`) — verify `tsc`
- [x] 5.6 Create `app/dashboard/page.tsx` (session check → `redirect('/login')`; unlinked → message + sign-out form; else `resumenMensual` with `parseMonthParam(searchParams.mes)`) and `app/dashboard/supabase-dashboard.tsx` (D14: charges via `selectUpcomingCharges`, actions from 5.5, `changeLanguage`, cycle navigation through `?mes=`, `nextCycle` always supplied and disabled by the shared month selector on the cycle in progress, no `openSavingsHistory`, no `notice`) — verify `tsc` and `npm run build`
  - 2026-09-24: `page.tsx` passes the raw `mes` to `resumenMensual`, which calls `parseMonthParam(mes, currentMonth)` itself (the clamp needs the cycle in progress, which only `resumenMensual` computes). `createCategory`/`updateCategory` return `{ ok: false }` for a duplicate name instead of throwing, because production builds hide a thrown action's message; the wrapper rethrows `DUPLICATE_CATEGORY_NAME`. Verified on `next start`: `/` shows Demo/Entrar ("Log in" with `locale=en`), `/login` renders the e-mail field and the `?error=enlace` message, `GET /auth/confirm` → 307 `/login?error=enlace`, `/dashboard?mes=2026-08` → 307 `/login`; no Supabase code in `.next/static/chunks`.

## 6. Test user seed

- [x] 6.1 Write `supabase/seed/test-user.sql` (D15): header with the run instructions, `do` block with the placeholder e-mail first, raise when the auth user is missing, upserts for `usuarios`/`categorias`/`movimientos_recurrentes`, `presupuestos` rows for the current cycle and each of the five past ones keyed by each cycle's first day, deterministic-id transactions for the current cycle and five past cycles reaching the totals in `test-user-seed` → *The seeded cycle is believable and current* — verify by static review that every `on conflict` targets an existing unique constraint and that no `insert` lacks one
  - 2026-09-24: static review OK (`usuarios (telefono)`, `categorias (usuario_id, nombre)`, `movimientos_recurrentes (usuario_id, nombre)`, `presupuestos (usuario_id, categoria_id, periodo)`, `transacciones (id)`). Dry run on the real project inside one `do` block aborted by a final `raise` (temporary auth user, script body twice; afterwards 0 auth users, 0 seed rows): both runs 45 tx / 7 cat / 7 def / 18 budgets / 1 usuario; cycle 2026-08-26; gasto 1700 (1025 recurring), ingreso 2820, ahorro 146, accumulated 2646; comida 310/400, ocio 130/150, transporte 130/100; past cycles 1750/1450/1600/1550/1400 (k=1..5); budgets keyed 2026-03-26…2026-08-26; charges on 09-01…09-22 with `ciclo_mes` 2026-08-26.
- [x] 6.2 Create the auth user and run the script on the real project (proposal → *Manual steps* 5–6) — verify it completes without error and `select count(*) from transacciones where usuario_id = (select id from usuarios where email = '<placeholder>')` matches the number of seed rows
  - 2026-09-24: auth user created through the MCP with SQL (`auth.users` confirmed + `auth.identities` provider `email`, empty-string tokens), not from the dashboard; the repo file keeps the placeholder and the run swapped only `v_email`. Result: 45 `transacciones` (= 45 seed rows), 7 categories, 7 definitions, 18 budgets, `usuarios.auth_user_id` linked. RLS check as `authenticated` with that `sub` (rolled back): sees 1 `usuarios` row, 45 transactions, 7 categories, 0 of Brian's; `copiar_presupuestos_ciclo` runs as the user and the current cycle keeps its 3 rows.
- [x] 6.3 Run the script a second time — verify every per-table count for the test user is unchanged (`test-user-seed` → *Second run*)
  - 2026-09-24: second run → 45 / 7 / 7 / 18 / 1 usuario, unchanged.

## 7. Automated verification

- [x] 7.1 Add `tests/home.spec.js`: `/` shows "Demo" and "Entrar", each navigates to its route, and no request to `*.supabase.co` is made — verify `npx playwright test tests/home.spec.js` passes with placeholder `NEXT_PUBLIC_SUPABASE_*` values in the environment
- [x] 7.2 Add `tests/login-access.spec.js`: `/login` renders the e-mail field and submit; "brian" is refused without a request; `/dashboard` and `/dashboard?mes=2026-08` redirect to `/login`; `/demo` still renders the sample notice — verify the spec passes
- [x] 7.3 Run `npx tsc --noEmit`, `npm run lint`, `npm run build` and the full `npm test` — verify all green, with the pre-existing `/demo` specs unchanged
  - 2026-09-24: `tsc`, `npm run lint` (only the existing `PendingRecurringDecision` warning in `lib/whatsapp/adapter.ts`), `npm run build`, `npm run test:unit` green. 7.1 + 7.2 with placeholder `NEXT_PUBLIC_SUPABASE_*`: 24/24 on chromium/firefox/webkit. Full `npm test`: 232/237; the 5 failures were pre-existing `/demo` specs on firefox/webkit under parallel load (`budget-envelope`, `category-create` duplicate name, `recurring-motion-a11y` reduced motion, `reorder-mode` ×2). 4 passed on `--last-failed`; the remaining one (`recurring-motion-a11y` reduced motion, firefox) passes alone 3/3 here and 2/2 on a clean `HEAD` worktree, so it is flaky under load, not a regression. No existing spec was edited.

## 8. Manual verification against the real project

- [x] 8.1 Complete proposal → *Manual steps* 2–4 (email provider, URL configuration, magic-link template) — verify the template body contains `/auth/confirm?token_hash={{ .TokenHash }}&type=email`
  - 2026-09-24: steps 2 (sign-ups off) and 3 (Site URL `http://localhost:3000`, redirect `http://localhost:3000/auth/confirm`) done by Brian in the dashboard. Step 4 is not possible: Supabase no longer lets the templates be edited without custom SMTP. Replaced by the design D5 amendment (`?code=` + `exchangeCodeForSession`, `emailRedirectTo`), so the template check does not apply until SMTP is set up.
- [x] 8.2 Log in with the test e-mail: request the link on `/login`, open it from the inbox — verify landing on `/dashboard` with the seeded figures (2.820 € / 1.700 € / 146 € / 864 €, accumulated 2.646 €, "comida" 310 of 400, "transporte" 130 of 100, six bars) and the account menu showing the seeded name and phone
  - 2026-09-24: Brian logged in with the real magic link (default template, `?code=` path) and reached `/dashboard`. Figures checked with Playwright on a second session (a one-time token inserted in `auth.one_time_tokens` and consumed through `/auth/confirm?token_hash=`): margin 864 €, income 2.820 €, expenses 1.700 €, savings 146 €, accumulated 2.646 €; Comida 310 € de 400 €, Ocio 130 € de 150 €, Transporte 130 € de 100 € ("30 € por encima"); comprometido 1.025 €; six bars 1.400/1.550/1.600/1.450/1.750/1.700; cycle "septiembre · en curso · 26 ago–25 sept"; account menu "Usuario de prueba" / +10000000001; 0 browser requests to a Supabase host.
- [x] 8.3 Exercise every operation once and reload after each: add/edit/delete/restore an expense, an income entry, a savings deposit and a withdrawal; create/rename/recolour/re-budget/clear the budget of/reorder/delete a category (delete onto another; after the re-budget and the clear, the previous cycle keeps its own budget, and the cleared one is not copied back on reload); create a recurring expense from the sheet, edit its definition, stop it, delete it — verify each result survives the reload as `dashboard-data` → *Every dashboard operation persists* describes, and that the two-user and foreign-id scenarios hold using Brian's seeded user as the second account
  - 2026-09-24: Playwright on `/dashboard` (session from a one-time token), a reload after every operation, 0 browser requests to a Supabase host. Expense "Panadería" 20 → comida 330 of 400, expenses 1.720, margin 864; edit 25 → 1.725; delete → gone after reload; "Café" delete + undo → back with 62,4 and its date. Income "Bonus" 300 → 3.120 / margin 1.164; edit 350 → 3.170; delete → gone; "Freelance" delete + undo → back. Deposit 50 → savings 196, accumulated 2.696, margin 814; withdrawal 30 → "−30 €", 166 / 2.666 / 844, income and expenses unchanged. Rows in the DB: EUR, `T12:00:00Z`, withdrawal `-30`, deletes as `borrado_en`. Category "Viajes" with 200 → last, 0 of 200, margin 644, one row in `2026-08-26` only; " comida " → duplicate message, nothing created; rename → "Escapadas"; recolour → "Azul eléctrico" after reload; comida 600 → 310 of 600, margin 444, `?mes=2026-08` still 450 of 400; clear → no bar after two reloads, margin 734, marker (`monto` null) in `2026-08-26`, 400 kept in `2026-07-26`; reorder Comida 4→5 persisted; "Hogar" onto "Compras" → Limpieza and Decoración in Compras (190), Limpieza's sheet names Compras, expenses 1.700; empty "Escapadas" deleted with its budget rows. Recurring "Netflix" 12,99 day 5 in Compras → one row, "Netflix · día 5, 12,99 €, Cobrado", sheet opens with day 5; edit 15 → charge (taken) keeps 12,99, "esperado 15 €"; stop → still listed; delete → no Netflix anywhere, charge soft-deleted and unlinked. Pending branch ("Pendiente" 10, day 25): edit 20 → charge rewritten, expenses +10; stop → `activo = false`, `repeticiones_insertadas = 1`. Two users: the test user's page shows none of Brian's names. Foreign id: the 13 server actions called with Brian's ids (category update/delete, delete onto his category, reorder, definition stop/update/delete, expense/income update/soft-delete/restore) reject with `not-found` / `invalid-order`; `createExpense` into Brian's category was accepted → fixed by 3.4, rejected afterwards. Brian's categories, definitions and budgets unchanged (md5 before/after); he has no transactions, so movement ids were tested with a nonexistent id.
- [x] 8.4 Navigate cycles: previous, next (disabled on the cycle in progress), the picker, `?mes=abc`, a `?mes=` two months ahead (shows the cycle in progress, and no `presupuestos` row exists after the current cycle) — verify `dashboard-data` → *Figures are scoped to one billing cycle* scenarios
  - 2026-09-24: `/dashboard` → "septiembre · en curso · 26 ago–25 sept", both "Ciclo siguiente" disabled; previous → `?mes=2026-08`, "agosto", "26 jul–25 ago", not in progress, expenses 1.750, next enabled; next → back in progress; picker: oct–dic disabled, "jun" → `?mes=2026-06`, "26 may–25 jun", 1.600; `?mes=abc` and `?mes=2026-11` → September in progress, next disabled. `max(presupuestos.periodo)` = `2026-08-26`, 0 rows after it.
- [x] 8.5 Open `/login` while signed in, sign out from the account menu, open `/dashboard` again, open a used magic link — verify the `web-access` scenarios *Signed-in visitor on login*, *Log out* and *Expired or reused link*
  - 2026-09-24: `/login` signed in → `/dashboard`; "Cerrar sesión" enabled in the account menu → `/`; `/dashboard` → `/login`; the already consumed token hash → `/login?error=enlace` with "El enlace venció o ya se usó. Pide uno nuevo."; `/dashboard` afterwards → `/login`.
- [x] 8.6 Create a second auth user without running the seed and log in with it — verify the "cuenta sin vincular" message and the sign-out action, with no figures
  - 2026-09-24: auth user created by SQL (confirmed, `auth.identities` provider `email`, empty-string tokens), no `usuarios` row; one-time token → `/dashboard` shows only the "cuenta todavía no está vinculada" message and "Cerrar sesión" (no € figure, no "Margen libre"); sign-out → `/`, `/dashboard` → `/login`. The user was deleted afterwards (0 users, identities, sessions, tokens left).
- [x] 8.7 Open `/demo` signed in with the network panel recording, open every panel and add an expense, an income entry and a savings movement — verify no request to a Supabase host and the sample figures unchanged
  - 2026-09-24: with the `sb-` session cookie present, `/demo` shows the sample notice and margin 864; 7 category panels, "Próximos cobros", Ingresos, Gastos and Ahorro opened; expense 20 → 1.720, income 300 → 3.120, deposit 50 → 196; after a reload 864 / 2.820 / 1.700 / 146. Only host contacted: `localhost:3000`; no demo row reached the database.
  - Afterwards the seed ran again: 7 categories / 7 definitions / 18 budgets and the 45 seed transactions. The cleanup before it also deleted 4 rows someone had loaded on the test user during the session (Brian: disposable data). The account now also holds 7 rows of May 2025 that were being loaded on it at the time, so its total is 52.
- [x] 8.8 Record the outcome of 8.1–8.7 in this file (a line per task, with the date) and update `CLAUDE.md` → *Estado actual*, *Proyección* and *Deuda técnica* (real data layer done for the web; `estado`, cron, null `dia_del_mes`, `repeticiones_insertadas` divergence, currency mixing, and 0015–0018 status; remove the "Supabase implementation of `BudgetRow`" entry left by `fix-free-margin-envelope-budgets`) — verify the debt list no longer says the contracts have no real implementation
  - 2026-09-24: 8.1–8.7 each carry their outcome line above. `CLAUDE.md` updated: *Estado actual* (web on Supabase, routes, `resumenMensual`, 0001–0019 applied, `0019`, the `dashboard-template.tsx` UI tweaks made outside the tasks, change 36/36), *Proyección* (bot, cron + `estado`, deploy + SMTP next), *Deuda técnica* (`estado` and cron updated; null `dia_del_mes`, `repeticiones_insertadas` divergence and currency mixing added; "Capa de datos real" and "Presupuestos por ciclo sin implementación real" removed; magic-link mail entry kept). `grep` finds no claim that the contracts lack a real implementation.
