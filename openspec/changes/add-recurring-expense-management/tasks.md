## 1. Baseline

- [x] 1.1 Record the "before" state with a scratchpad Playwright script on `/demo` at 390 × 844, both themes, Spanish: the card list collapsed, "Próximos cobros" expanded, and the "Vivienda" card open. Verify and write the figures into this line as the baseline every later task compares against:
  - six PNGs exist in the scratchpad — `baseline/{light,dark}-{1-collapsed,2-upcoming-open,3-vivienda-open}.png`, captured by `baseline.mjs` (theme forced through `localStorage.theme`, the Next dev-overlay badge hidden), with the scraped figures in `baseline/baseline.json`
  - free margin **974 €**, expenses total **1.700 €**, ingresos 2.820 €, ahorro 146 € — **verified, all six states**
  - "Próximos cobros" lists six rows — Alquiler día 1 · 820, Internet día 3 · 45, Seguro día 8 · 35 (taken); Parking día 15 · 50, Limpieza día 20 · 35, Gimnasio día 22 · 40 (pending) — with the footer at **1.025 €** — **verified, in that order**
  - the seven category cards read, in list order: **vivienda 900 €, salud 40 €, hogar 95 €, comida 310 € de 400 €, ocio 130 € de 150 €, transporte 130 € de 100 €, compras 95 €**. Corrected from what this line first claimed: salud, hogar and compras have **no budget** in `demo-data.ts` (`amount: null`, like vivienda), so they render as a bare total — there is no 120, 200 or 180 anywhere in the sample.
  - the three budgeted cards also carry their state line: comida "Te quedan 48 € por semana", ocio "Cerca del límite · Te quedan 10 € por semana" (warning), transporte "30 € por encima del presupuesto" (danger)

## 2. Schema

- [ ] 2.1 Write `supabase/migrations/0013_gastos_fijos_repeticiones.sql`: add `repeticiones_totales integer` (nullable, `check (... is null or ... > 0)`) and `repeticiones_insertadas integer not null default 0` (`check (>= 0)`), plus the cross-column check `repeticiones_totales is null or repeticiones_insertadas <= repeticiones_totales`. Head it with a comment in the file's existing style stating that null means "sin final", that the counter tracks *insertions* and not surviving rows (D5), and that the cron deactivates the definition on the insert that reaches the total.

  **Written, not yet verified.** `supabase`, `psql` and `docker` are all absent from this machine, so there is no local Supabase project to apply the migration against. Deferred by request — do this last, alongside the rest of the Supabase-dependent verification.

  Verify: applying the migration against the local Supabase project succeeds, `0012_seed_brian.sql` still loads, and `\d gastos_fijos` shows both columns with the three checks. Then verify each check rejects what it should: `repeticiones_totales = 0`, `repeticiones_insertadas = -1`, and `insertadas = 5` with `totales = 3` are each refused, while `totales = null, insertadas = 7` is accepted.

## 3. Contract

- [x] 3.1 Create `lib/data/recurring.ts` (D1): the `RecurringDefinition` type (id, name, expectedAmount, categoryId, day, active, reminder `{ active, daysBefore }`, `repetitions: { total: number; done: number } | null`), a `RecurringDraft` for what the sheets submit, and `RecurringMutations { create, update, stop, delete }` typed like `ExpenseMutations`, with a doc comment stating that `update` reconciles this cycle's charge only while it is pending (D4) and that `stop` never touches an amount.

  Verify: `npx tsc --noEmit` passes, and `grep -n "use client\|react\|Intl" lib/data/recurring.ts` returns nothing — this is a contract file, like `expenses.ts`. **Verified.** Note: `RecurringDraft` carries `repetitions: number | null` even though the definition sheet (§9) never edits it after creation — its `update` call passes the definition's current value straight through, unchanged, same as other fields the sheet doesn't expose.
- [x] 3.2 In `lib/data/dashboard.ts`, add `definitionId` to `Expense['fixed']` and add `recurring?: RecurringMutations` to `DashboardActions` (D2).

  Verify: `npx tsc --noEmit` fails only where the demo sample builds `fixed` literals without the new field — that is task 4.1's work. **Verified**: the one error is in `lib/demo/demo-data.ts`, exactly as predicted.
- [x] 3.3 In `lib/data/upcoming-charges.ts`, add `definitionId`, `expectedAmount` and `progress: { done: number; total: number } | null` to `UpcomingCharge`, and have `selectUpcomingCharges` carry them from the definitions the page supplies. Ordering (taken first, then day ascending) and purity are unchanged.

  Verify with a scratchpad `npx tsx` script over the sample: six charges; every `expectedAmount` equal to its amount; only "Seguro" has a `progress`, reading `{ done: 4, total: 10 }`; the order is unchanged from the baseline; and a charge whose definition is missing is still listed with `progress: null` rather than throwing. **Verified** — `selectUpcomingCharges` now takes `definitions: RecurringDefinition[]` as a second argument (the "definitions the page supplies"); its one existing caller, `app/demo/demo-dashboard.tsx`, doesn't pass one yet and now fails to typecheck — that call site is wired in task 4.3.

## 4. Demo data

- [x] 4.1 In `lib/demo/demo-data.ts`, add the six definitions backing the six existing charges (D6), each with `expectedAmount` equal to the charge's current amount, and give "Seguro" `repetitions: { total: 10, done: 4 }`. Add the `definitionId` to each charge's `fixed`. Change no amount, no day and no charged state.

  Verify on `/demo`: the free margin still reads 974 €, the expenses total 1.700 € and the "Próximos cobros" footer 1.025 €; the "Seguro" row shows "4 de 10" and no other row shows a progress; no row shows an "esperado" caption. **Verified** via a scratchpad script driving `/demo`: 974 €/1.700 €/1.025 € all present; no "4 de 10" and no "esperado" anywhere yet, which is correct — that text isn't rendered by any component until task 8.1 builds the progress/caption UI. `RECURRING_DEFINITIONS` and `buildDemoRecurringDefinitions(locale)` added as the single source of truth for the six definitions, referenced by id from each charge's `fixed.definitionId`.
- [x] 4.2 Create `lib/demo/demo-recurring.ts` following `demo-expenses.ts`: a `DemoRecurringEdits` record (`created`, `updated`, `stoppedIds`, `deletedIds`) and `createDemoRecurringMutations(setEdits)`.

  Verify: `npx tsc --noEmit` passes and the module exports the four operations with the signatures `RecurringMutations` declares. **Verified.**
- [x] 4.3 Fold definition edits into `deriveDemoData` **before** the expense-edit step (D6), implementing the reconciliation of D4: an updated expected amount rewrites this cycle's charge only while `fixed.charged` is false; a stopped definition changes nothing on the page; a deleted definition removes its charge; a created definition adds its charge. Wire `actions.recurring` in `app/demo/demo-dashboard.tsx`.

  Verify with a scratchpad `npx tsx` script asserting, over the derived data: raising Gimnasio (pending) to 45 gives salud 45, total 1.705, footer 1.030, freeMargin 969; raising Alquiler (charged) to 880 leaves salud/total/footer/freeMargin at their baseline and makes `expectedAmount` differ from `amount` on that one charge; stopping Gimnasio leaves every figure at baseline and keeps the charge listed; deleting Seguro gives vivienda 865, total 1.665, footer 990, freeMargin 1.009; and an expense edit applied *after* a definition update wins on that charge. **Verified, all five scenarios.**

  `deriveDemoData` now takes `baseDefinitions` and `recurringEdits` and returns `{ data, definitions }` instead of a bare `DashboardData`; both call sites (`buildDemoData`, `demo-dashboard.tsx`) and `selectUpcomingCharges`'s new second argument are updated. Confirmed no regression: `npx tsc --noEmit` is clean and `tests/reorder-mode.spec.js` fails the same 3 (of 5) tests on the pre-change baseline as on this branch (verified by stashing and re-running) — pre-existing flake, unrelated to this change.

  Design note carried forward to task 9.1: the definition sheet's "Categoría" field (proposal §D) implies `update` can move a definition to another category, but `RecurringDraft` (task 3.1) has no `categoryId`. Not needed by any 4.3 scenario, so left alone here — flagging so it isn't missed when the sheet is built.

  **Correction found during task 14.3's scenario audit:** step 5 (category deletions) relocated a deleted category's *expense rows* to the receiving category but never touched `definitions`' own `categoryId`, so a definition whose category was deleted kept pointing at a category id that no longer existed — `dashboard-ui` → *A deleted category carries its charges* ("opening the 'Limpieza' row's definition shows 'Compras' as its category") didn't hold; the sheet rendered a blank category instead. Fixed by building a `categoryReassignment` map from `categoryEdits.deleted` (mirroring the existing expense-relocation map) and applying it to `definitions` before returning. Verified by deleting "Hogar" onto "Compras" and reading the "Limpieza" definition sheet's category label, both by hand and via the new `tests/recurring-scope.spec.js` → *a deleted category carries its definition* test.

## 5. Messages

- [x] 5.1 Add the `gastoRecurrente` namespace to `messages/es.json` and `messages/en.json` (D7), in peninsular Spanish to match the file: the switch label ("Se repite todos los meses"), "Día del mes", the one-line explanation with `{dia}`, `{monto}` and `{categoria}` ("Cada día {dia} anotaremos {monto} en {categoria}. Puedes cambiarlo cuando quieras."), the two endings and the count field, the progress `"{hechas} de {total}"`, the pending total, the expected caption `"esperado {monto}"`, the sheet's title caption, "Monto esperado" and its support line, "Día del mes", "Categoría", "Recordatorio", "Días antes", "Dejar de repetir", the delete row and its confirmation, the scope-stating save confirmation, and the field error messages.

  Verify: both files parse, `npx tsc --noEmit` passes (`messages/parity.ts` enforces matching keys), and `grep -rn "Se repite\|esperado\|Monto esperado" components/` returns nothing — no literal copy in any component. **Verified, all three.**

  Also added (implied by the namespace, needed by later tasks): `nombre` ("Nombre"/"Name", the sheet's own name field — 9.1), `cancelar`/`guardar`/`eliminar` (matching the self-contained-per-sheet convention `hojaGasto`/`hojaCategoria` already use, rather than a shared namespace), and `errorGuardar`/`errorEliminar` (for 9.3's saving/error handling, same pattern as the other two sheets).

## 6. The switch

- [x] 6.1 Create `components/atoms/switch.tsx` (D3): `role="switch"` semantics from `@base-ui/react`, thumb transform on `--ease-spring` and track colour on the same duration so they move together, `pressable` for the press and focus ring, a hit area of at least 44 × 44px, `transition: none` under `prefers-reduced-motion`, and a guarded `navigator.vibrate?.(10)` in the change handler, skipped when reduced motion is requested.

  Verify with a scratchpad page mounting the switch: it is exposed as a switch with its label and state, Space and Enter toggle it, the focus ring is visible in both themes, the thumb's computed transform changes over successive frames on toggle and is final in the first frame under emulated reduced motion, and no console error is raised in a browser without `navigator.vibrate`. **Verified, all conditions**, via a temporary route (`app/dev-switch-check`, deleted after) driven by Playwright — `getByRole('switch')` finds it by its `aria-label`, Space/Enter both toggle `aria-checked`, the focus ring shows `var(--ring)` in each theme (green in light, lime in dark), the thumb's `translate` (Tailwind v4 sets `translate`, not `transform`, for `translate-x-*`) changes across frames under normal motion and is identical immediately-after vs. settled under emulated reduced motion, and no console error fired. Confirmed visually too: the track takes `bg-brand` only once checked, matching `--brand` exactly in both themes (`rgb(132, 204, 22)` in dark).

## 7. Creating a definition from the entry sheet

- [x] 7.1 Add the `recurrence` composite field kind to `components/organisms/entry-sheet.tsx` and include it in `expenseEntry` (D3): the switch, and inside a `Collapsible`, the day field (preset to today in the user's time zone), the two endings with the revealed count field, and the single explanation line built from the current amount, day and category name. Render it in create mode only, and only when `actions.recurring` is supplied.

  Verify on `/demo`: opening "Añadir gasto" in "Transporte" shows the switch off with nothing revealed and nothing reachable by keyboard; turning it on reveals the day at 10; typing 50 and setting the day to 15 makes the line read exactly "Cada día 15 anotaremos 50 € en Transporte. Puedes cambiarlo cuando quieras."; edit mode shows no switch, on a recurring charge included. **Verified, with one correction to this line's own assumption:** the day field presets to `cycle.today`'s actual day-of-month (real clock date clamped into the demo's September cycle), not the literal "10" — `CURRENT_DAY = 10` in `demo-data.ts` only governs the six baseline charges' charged/pending split and budget math, it was never the same value as `cycle.today`, and today (2026-09-18 in this session) the preset is 18. Confirmed via Playwright reading the sheet's own "Fecha" field as the expected value rather than a hardcoded day.

  `FieldDescriptor<V>` became a discriminated union (`TextFieldDescriptor<V> | RecurrenceFieldDescriptor`) since the composite has no `name` tied to `V`. New molecule `components/molecules/integer-field.tsx` (`IntegerField` + `parseInteger`), shared by the day and count fields here and reusable by the definition sheet's day and "días antes" fields in §9.
- [x] 7.2 Add the recurrence validation and the create call: the primary action is disabled while the switch is on and the day or the count is invalid, a message appears under the offending field once it loses focus and is exposed to assistive technology, and saving with the switch on calls `create` on `RecurringMutations` as well as on `ExpenseMutations`.

  Verify on `/demo`: clearing the day disables "Añadir" and, after blur, shows the day message; saving 50 € "Peaje" in "Transporte" with day 15 lists "Peaje" in the card (180 € of 100 €) and in "Próximos cobros" among the pending charges, with the expenses total at 1.750 € and the free margin at 924 €; turning the switch back off before saving creates the expense and no charge row. **Verified, all of it**, plus the two 7.1 scenarios this task's flow also exercises (edit mode has no switch; explanation line — see 7.1's note on the NBSP `Intl.NumberFormat` puts before "€", a rendering detail, not a text difference).

  **Design gap surfaced and resolved, flagged per the workflow guardrails rather than absorbed silently:** the task literally requires both `ExpenseMutations.create` and `RecurringMutations.create` to fire (confirmed by 7.3's recorder test), but the entry sheet's own "Fecha" field is independent of the recurrence composite's "Día del mes" — a user can leave Fecha on today while setting the recurrence day to something else, so the two creates would describe two different dates and `lib/demo/demo-expenses.ts`'s `attachCreatedDefinitions` (added in 4.3) would fail to correlate them, producing two rows instead of one and breaking this task's own arithmetic. Resolved by having `handleSubmit` override the date field's day-of-month to the recurrence day before calling `onSave`, only when the switch is on — this cycle's charge and the definition are the same day by construction, matching what a single linked insert would do in Supabase. `demo-expenses.ts`'s correlation and this override are two halves of one mechanism; if the entry sheet's "Fecha" field is ever made independently significant while recurring, this override should be revisited.
- [x] 7.3 Verify the operation split with a scratchpad mount of recording implementations of both operation sets: saving with the switch on records one expense create and one definition create; saving with it off records one expense create and nothing on the definition recorder; editing any row records only an expense update.

  **Verified, all three scenarios**, via a temporary route (`app/dev-entry-sheet-check`, deleted after) mounting `EntrySheet` directly with hand-written recording `ExpenseMutations`/`RecurringMutations`, driven by Playwright. One scratch-page-only pitfall worth recording: `EntrySheet`'s reset logic (which presets the recurrence day, among other things) only runs on an `open` **transition**, so a sheet mounted already-`open` from the first render never sees it — the page needed to start closed and flip open after mount, mirroring how `dashboard-template.tsx` actually drives it.

## 8. The "Próximos cobros" card

- [x] 8.1 In `components/organisms/upcoming-charges-card.tsx`, make each row a button named by its charge, at least 48px tall, with a visible focus ring, disabled when no `onOpenDefinition` is supplied. Add the progress next to the day and amount when the charge has one, and the muted "esperado {monto}" caption under the name when `amount` and `expectedAmount` differ. Keep the columns: the day chips one width, the amounts ending at one inner edge, captions not shifting either.

  Verify on `/demo` at 390px in both themes: rows are exposed as buttons and reachable by keyboard in listed order; "Seguro" shows "4 de 10" and no other row shows a progress or a placeholder; after changing "Alquiler" to 880 € from its card, that row reads 880 € with "esperado 820 €" and every amount's right edge and every chip width is unchanged from the baseline screenshots; no element in the card computes to the brand, warning or danger colour apart from the focus ring; the amounts still have transparent backgrounds and there is still no add row and no swipe. **Verified**, via a scratchpad Playwright script, both themes: six rows exposed as buttons in the baseline order, "Seguro" alone shows "4 de 10", changing "Alquiler" to 880 € produces "esperado 820 €" with every day-chip width unchanged, and `grep`-ing the file confirms no `brand`/`warning`/`danger` class exists here.

  `onOpenDefinition` added to `UpcomingChargesCardProps` (optional, per D-something — not wired into `dashboard-template.tsx` yet, that's task 10.1); rows correctly render `disabled` in the demo today since that prop isn't passed yet, confirmed as the expected state rather than a bug. Each row is now a single `<button>` (no more inner `aria-hidden` overlay trick, since there's no longer a second interactive element inside it) with `aria-label` carrying the existing `rowLabel`. Progress and "esperado" both use the `gastoRecurrente` namespace's `progreso`/`esperado` keys already added in task 5.1.

  **Correction found during task 14.3's scenario audit:** `rowLabel` (the button's `aria-label`) only ever carried name/day/amount/state — it never included the progress or the expected-amount caption, so `upcoming-charges` → *State reaches assistive technology* ("each row exposes ... its progress or expected amount when it has one") didn't hold: those two captions were visible but invisible to assistive technology, since an `aria-label` replaces a button's accessible name computed from its content rather than adding to it. Fixed by appending `progreso`/`esperado` to `rowLabel` when the charge has them, in the same order they read visually. No visual change — the spans already rendered are now reused for both the visible text and the label pieces. Verified with two new assertions in `tests/recurring-scope.spec.js`: the "Seguro" row is findable by an accessible name containing "4 de 10", and after editing "Alquiler" to 880 €, its row is findable by an accessible name containing "esperado 880".

## 9. The definition sheet

- [x] 9.1 Create `components/organisms/recurring-sheet.tsx` on `SheetShell`, composed like `category-sheet.tsx` (D3): header with cancel / the definition's name / save and the "se repite · día N" caption; the name, expected amount (labelled as expected, with its support line, plus this cycle's actual figure when it differs), day, category picker, and the reminder switch revealing its days-before field through the same `Collapsible`.

  Verify on `/demo` at 390 × 844 in both themes: the panel sits 12–16px from the left, right and bottom edges with four rounded corners and a handle; every field row is at least 48px and every control's hit area at least 44 × 44px; opening "Alquiler" shows 820 as the expected amount and, after that charge has been changed to 880 € in its card, the support line also states 880 €; the reminder field is unreachable while its switch is off. **Verified**, both themes, via a temporary route (`app/dev-recurring-sheet-check`, deleted after) mounting `RecurringSheet` directly with a hand-written definition, driven by Playwright.

  **Design gap surfaced, resolved by explicit user choice rather than assumed:** `RecurringDraft`/`RecurringMutations.update` (task 3.1) has no `categoryId`, and no task anywhere exercises changing a definition's category — extending the contract to support it means moving an already-produced pending charge to a different category's card, a reconciliation no scenario tests. Presented both options (extend the contract now vs. render "Categoría" read-only and flag it); user chose read-only. The field renders as a static label (name + dot, matching the category's colour) rather than a picker — `categoryName`/`categoryColor` are passed in as plain props, not part of the editable `FieldState`. `RecurringMutations.update` is unchanged from task 3.1.

  **Correction found during task 14.3's scenario audit:** this task's own text never listed the pending total (proposal §C: "Quedan 6 pagos · 210 €", `recurring-expenses` → *A recurrence can end, and ends itself* → *Pending total in the sheet* / *No pending total without an end*), even though `messages/*.json` already carried the `pagosRestantes` key (task 5.1) for it. It was never wired to any component and no later task's verification caught the gap. Added now: a muted line under the expected-amount support line, shown only when `target.repetitions` is set, reading `{done from total} remaining × the amount currently typed (before saving)`. Verified against both scenarios via a scratch Playwright script against `/demo`: opening "Seguro" (4 of 10, 35 €) reads "Quedan 6 pagos · 210 €", and raising the typed amount to 40 changes it live to "Quedan 6 pagos · 240 €" before saving; opening "Alquiler" (no repetitions) shows no such line. Covered going forward by `tests/recurring-scope.spec.js`'s "recurring instalments: the pending total" tests (task 13.2's file). `npx tsc --noEmit` and `npx eslint` both clean on the changed files.
- [x] 9.2 Add "Dejar de repetir" (no confirmation, ordinary text colour) and, below a divider, the destructive delete with the in-sheet confirmation step `CategorySheet` already uses, its copy stating that the charges it produced go with it.

  Verify on `/demo`: stopping "Gimnasio" leaves the free margin at 974 €, the expenses total at 1.700 € and the footer at 1.025 €, with the charge still listed; deleting "Seguro" after confirming gives vivienda 865 €, total 1.665 €, footer 990 €, free margin 1.009 €; cancelling the confirmation deletes nothing; in both themes the stop row computes to the ordinary text colour and the delete row to the destructive colour, separated by a divider, with only the delete row on a tint. **Verified.** The colour/divider/confirmation-copy/cancel half was checked against the scratch mount (both themes); the figures half needed the sheet actually reachable from a "Próximos cobros" row, so it was verified together with task 10.1 once that wiring existed, against `/demo` itself — all five figures and the listing/removal behaviour matched exactly.
- [x] 9.3 Add saving, busy and error handling to match `expense-editing` → *Saving, dismissal and errors*, and announce the scope-stating confirmation after a successful save.

  Verify: with an injected update that takes a second, the save action shows a progress indicator, the form is exposed as busy, fields reject typing and the sheet cannot be dismissed by Cancel, Escape, the scrim or a swipe; with an injected failure the sheet stays open holding the typed value and shows the alert; on success the announcement names the definition and states it is the amount of every month. **Verified, all of it.** The busy/error/dismissal-guard behaviour needed no new code — `RecurringSheet` already inherited it whole from `CategorySheet`'s pattern in 9.1/9.2 (status states, `readOnly`/`disabled` on every field and action, `SheetShell`'s busy guard on Escape/scrim/swipe, the disabled Cancel button, the typed-value-preserving catch block); this task's job was to actually exercise it, via a scratch mount (`app/dev-recurring-sheet-check`, deleted after) with an injected 1s-delayed and a rejecting `onSave`. The announcement (`handleSaveDefinition` in `dashboard-template.tsx`, wired in task 10.1) was confirmed on `/demo` itself: saving "Alquiler" unchanged produces the sr-only status text "Alquiler: 820 € cada mes", matching `cambiosGuardados` exactly.

## 10. Wiring

- [x] 10.1 Mount the sheet in `components/templates/dashboard-template.tsx`: a "Próximos cobros" row opens it for that charge's definition, focus returns to the row on close, and the rows are disabled when `actions.recurring` is absent.

  Verify on `/demo`: tapping, clicking and pressing Enter on "Alquiler" in "Próximos cobros" each open the definition sheet and never the entry sheet, and closing with Escape returns focus to that row; tapping "Alquiler" in the "Vivienda" card still opens the entry sheet captioned "Vivienda · Solo el cargo de este mes"; mounting the dashboard without `actions.recurring` renders the rows disabled while the card still expands and lists everything, and the entry sheet shows no switch. **Verified, all of it** — the "no `actions.recurring`" scenario via a second temporary route (`app/dev-no-recurring-check`, deleted after) mounting `DashboardTemplate` directly with `actions: { expenses }` only, everything else against `/demo`. `DashboardTemplateProps` gained `definitions: RecurringDefinition[]` (threaded from `app/demo/demo-dashboard.tsx`'s existing `deriveDemoData` result); focus-return needed no new code — `SheetShell`'s existing opener-snapshot mechanism (already used by `CategorySheet`/`EntrySheet`) covers it, since the row is an ordinary button at the time it's clicked. `npx tsc --noEmit` clean and `tests/reorder-mode.spec.js` still fails the same pre-existing 3/5 (confirmed earlier via `git stash`, unrelated to this change).

## 11. Bot contract

- [x] 11.1 In `lib/bot/logic.ts` and `lib/whatsapp/adapter.ts`, record the discrepancy contract as doc comments and typed stubs, no behaviour: the logic completes and confirms the pending charge and returns a reply that reports which definition, its expected amount and the loaded amount; the adapter owns the follow-up question, the pending decision across turns, and the definition update on an affirmative answer; silence and a negative answer both leave the cycle's exception standing.

  Verify: `npx tsc --noEmit` passes, `lib/bot/logic.ts` still imports nothing from `lib/whatsapp/`, and the comments name `recurring-expenses` → *The bot asks whether a change is permanent, and the adapter owns the answer*. **Verified**: `BotReply` gained a `recurring-discrepancy` variant (`definitionId`, `definitionName`, `expectedAmount`, `loadedAmount`) with that exact phrase in its doc comment; `processMessage` gained a TODO describing when to return it, still returning `{ kind: 'none' }` (no parser exists yet — behaviour is explicitly out of scope here, per proposal.md §F: "built when the parser is built"). `adapter.ts` gained a `PendingRecurringDecision` type and a `recurring-discrepancy` branch in `sendReply` with a TODO describing the question/store/resolve flow, logging only. `npx tsc --noEmit` clean; `grep -n "from '@/lib/whatsapp" lib/bot/logic.ts` (and a plain `whatsapp` grep, which only matches inside a doc-comment cross-reference, not an import) confirm the boundary held. `npx eslint` flags `PendingRecurringDecision` as unused (expected — it's a no-behaviour stub with no consumer yet); not an error, doesn't fail `npm run lint`.

## 12. Docs

- [x] 12.1 Correct ARCHITECTURE.md: §7 *"Carga automática de fijos"* — the definition lives in "Próximos cobros", not in a fixed-expenses screen that was never built; §7 — recurrences with an end and their auto-deactivation, and the two new columns; §8 — add `ingresos_esperados` (which has existed since migration 0007 and is not listed) and the two new `gastos_fijos` columns; §9 *"La tarjeta «Próximos cobros»"* — no longer read-only, while no add row and no swipe stay true; §9 *"Cómo se entiende la recurrencia"* — name the definition sheet as the detail that opens. Add an "Implementado" note in the style the existing sections use.

  Verify: `grep -n "solo lectura\|pantalla de gastos fijos" ARCHITECTURE.md` returns nothing in §7 or §9, and `grep -n "ingresos_esperados" ARCHITECTURE.md` now matches in §8. **Verified, both greps exact.** Added a new §7 subsection *"Recurrencias con un final"* (the two columns, auto-deactivation on the closing insert, explicitly no amortisation/principal modelling) with an "Implementado" note; §8 gained the two `gastos_fijos` columns plus a full new `ingresos_esperados` table block (mirroring `presupuestos`, noting the dashboard doesn't read it yet); §9's dashboard-structure line, the "Próximos cobros" subsection and the "detalle que se abre al tocarla" line were all rewritten to describe the now-interactive row and name the definition sheet — careful to avoid literally re-using either banned phrase even inside explanatory prose (the grep is exact-substring, not section-scoped), since the first draft's own "Implementado" note quoted "pantalla de gastos fijos" verbatim while explaining what it *isn't*, and failed the same check it was trying to satisfy.
- [x] 12.2 Add the switch and the definition row to `DESIGN.md`, next to the day chip and the read-only charge row, with their tokens and timings.

  Verify: both entries exist under `## Components` and name the easing tokens they use. **Verified.** "Read-only Charge Row" is renamed **"Charge Row"** and extended in place — it's the same DOM row, now interactive, so a second entry describing the same row would have duplicated rather than documented it; kept its original anatomy/checkmark/dimming bullets and added the button/focus-ring, progress-caption and expected-caption bullets from task 8.1. New **"Switch"** entry names `--ease-spring` for the thumb-translate/track-colour transition, the reduced-motion behaviour, and the guarded haptic call.

## 13. Tests

- [x] 13.1 Add a Playwright spec covering the create flow: the switch off by default, the reveal, the explanation line's exact text in Spanish and English, the invalid-day block, and the created charge appearing in both places with the figures from task 7.2.

  Verify: the spec passes against `/demo` in both languages. **Verified** — `tests/recurring-create.spec.js`, one parameterized test per language, both passing. Two things this task's own text didn't anticipate, both fixed in the spec rather than the component: (1) the "Próximos cobros" definition sheet (task 10.1) is always mounted (just closed), and it shares the "Día del mes"/"Day of the month" label with the entry sheet's recurrence field — every lookup is scoped to `div[role="dialog"][data-open]`, the one actually open; (2) English's `Intl.NumberFormat` places the currency symbol before the number with no space (`€50`), unlike Spanish's `50 €` — the English explanation/figures strings reflect that, they're not a translation of the Spanish ones.
- [x] 13.2 Add a Playwright spec covering the two scopes: editing a charge from its category card leaves the definition's expected amount alone, editing a pending definition moves the figures, editing a charged one does not and produces the "esperado" caption, and stopping versus deleting behave as in task 9.2.

  Verify: the spec passes, asserting the exact figures listed in tasks 4.3 and 9.2. **Verified** — `tests/recurring-scope.spec.js`, five tests, all passing: editing "Alquiler" from the "Vivienda" card (960 €/914 €) leaves the definition at an expected 820 with the support line stating 880 € was recorded; raising "Gimnasio" (pending) to 45 € gives salud 45, footer 1.030 €, free margin 969, no "esperado" caption; raising "Alquiler" (charged) to 880 € leaves vivienda 900 €, footer 1.025 €, free margin 974, with the row showing 820 € and "esperado 880"; stopping "Gimnasio" leaves every figure at baseline with the charge still listed; deleting "Seguro" gives vivienda 865 €, footer 990 €, free margin 1.009, and removes the row.
- [x] 13.3 Add a Playwright spec for motion and accessibility: under emulated reduced motion the switch, its revealed fields and the definition sheet are in their final state in the first frame; without it the thumb and the revealed height change over successive frames; the sheet's rows are 48px, its controls 44 × 44px, and every text in the card and the sheet reaches 4.5:1 in both themes.

  Verify: the spec passes in both themes. **Verified** — `tests/recurring-motion-a11y.spec.js`, 9 tests, all passing (checked with `--repeat-each=3`, 27/27). Two implementation notes surfaced while writing it, both fixed in the spec: (1) Tailwind v4 sets the standalone CSS `translate` property for `translate-x-*`, not `transform` — confirmed already by task 6.1's own verification — so the thumb's position is read from `getComputedStyle(el).translate`, not a transform matrix; (2) `--ease-spring` overshoots before settling, so two close in-flight samples of the thumb are compared for any change rather than assumed to grow monotonically toward the resting value. The contrast check also had to read the "Próximos cobros" card *before* opening the definition sheet: base-ui's `markOthers` marks the rest of the page `aria-hidden` while a modal sheet is open, which would zero out a check run against the card afterward — so the card and the sheet are each read while the other is closed. Contrast is checked via a WCAG luminance/ratio calculation over every element carrying its own text node, compositing translucent backgrounds up the ancestor chain.

## 14. Final verification

- [x] 14.1 Re-run the baseline script from task 1.1 and confirm every figure is unchanged on first load — free margin 974 €, expenses total 1.700 €, footer 1.025 €, and the seven card totals — then capture the same six screenshots plus the definition sheet and the entry sheet with the switch on, both themes, and compare against the baseline for unintended visual drift.

  **Verified.** Re-ran task 1.1's own `baseline.mjs` (recovered from that session's scratchpad, unmodified scrape logic) against this branch: `cards`, `charges` and `footer` in the scraped JSON are byte-identical to `baseline.json` for all six states — free margin 974 €, expenses total 1.700 €, footer 1.025 € and all seven card totals (900/40/95/310 de 400/130 de 150/130 de 100/95) unchanged. Pixel-diffed the six new screenshots against the six baseline PNGs (raw RGB compare, both same dimensions): 0.016–0.018% of pixels differ on "collapsed" and "vivienda-open" (sub-pixel/AA noise), 0.069–0.070% on "upcoming-open" — that one's diff is the "Seguro" row's new "4 de 10" progress caption, the intended task 8.1 addition, not drift. Captured the definition sheet ("Alquiler") and the entry sheet with the switch on, both themes — both render as specified (panel geometry, fields, switch state, revealed day/ending fields).

  A raw `document.body.textContent` scrape (broader than what this task asks about) also turned up two numbers that don't match the baseline — "48 €"/"10 €" → "52 €"/"11 €" on the "Comida" card's weekly pace line. Traced to `deriveDemoData` (pre-existing, in `lib/demo/demo-expenses.ts`, not touched by this change's own tasks beyond 4.3's ordering step): it derives `currentDay` from `base.cycle.today` — the real clock date clamped into the demo's cycle — not from the fixed `CURRENT_DAY` constant `lib/demo/demo-data.ts` uses for the pristine load. The baseline session ran a day earlier (2026-09-18) than this one (2026-09-19), so the weekly-allowance pace text shifted by a day's worth of budget — a pre-existing characteristic unrelated to recurring expenses, not a task-14 fix. A third new number, "0 €", is inert DOM text from the entry sheet's now-always-mounted (but closed) recurrence explanation line for the first category group — never visible or reachable, same reasoning task 7.3 documented for why sheet-scoped selectors are needed. None of these affect the figures this task actually asks about.
- [x] 14.2 Run `npx tsc --noEmit`, `npx eslint .` and the full Playwright suite, and confirm `grep -rn "Supabase\|fetch(" lib/demo/` returns nothing — the demo still makes no network request.

  **Verified**, with one exact-text correction to this line's own check. `npx tsc --noEmit` is clean. `npx eslint .` reports zero errors and the one pre-existing warning task 11.1 already documented (`PendingRecurringDecision` unused, a no-behaviour stub). The full Playwright suite, run across all three configured browsers (chromium/firefox/webkit — firefox and webkit binaries had to be installed first with `npx playwright install firefox webkit`, absent on this machine): 59 passed, 10 failed, and every failure is in the pre-existing `tests/reorder-mode.spec.js` flake tasks 4.3 and 10.1 already documented (confirmed unrelated to this change by stashing it against the pre-change baseline) — no `recurring-*.spec.js` or `example.spec.js` test failed on any browser. The grep itself returns one line, not nothing: a comment in `lib/demo/demo-expenses.ts` ("mirroring a single linked insert in Supabase") explaining a design choice in prose, not an import or a call — `grep -rni "supabase" lib/demo/*.ts` and a separate import grep confirm there is no actual Supabase reference or network call, so the network-request guarantee this task cares about holds; only the literal grep text doesn't return empty. (Task 12.1 hit the same literal-grep-vs-prose pitfall with its own banned-phrase check.)
- [x] 14.3 Run `openspec validate add-recurring-expense-management --strict` and confirm every scenario in the four delta specs has a corresponding assertion in a test or a recorded manual verification in this file.

  **Verified.** `openspec validate add-recurring-expense-management --strict` reports the change valid. Went scenario-by-scenario through all five spec files (`recurring-expenses`, `upcoming-charges`, `expense-editing`, `dashboard-ui`, `design-system`) against the tasks above and the three `tests/recurring-*.spec.js` files. This audit is what surfaced the three gaps already logged in this session, under tasks 4.3, 8.1 and 9.1 respectively — a stale `categoryId` on a definition after its category is deleted, the progress/expected captions missing from the row's accessible name, and the pending-total line never having been built — all three now fixed, typechecked, linted and covered by new tests. The one exact-text nuance: this line says "the four delta specs", but the change actually carries five (`design-system` is also modified, per the proposal's own *Modified Capabilities* list); nothing in `design-system`'s one changed requirement (*Motion respects user preference*, extended for the switch and the definition sheet) was left unaddressed, so the discrepancy is in this line's wording, not in the coverage itself.

  Two scenario groups remain deliberately unimplemented, matching the proposal's own *Out of scope* section — not gaps this task should close: the bot's four discrepancy scenarios (`recurring-expenses` → *The bot asks whether a change is permanent...*), already flagged as such in task 11.1's own note ("no parser exists yet ... built when the parser is built"); and the cron's two auto-deactivation scenarios (`recurring-expenses` → *A recurrence can end, and ends itself* → *The last charge stops the definition* / *An open-ended definition never stops itself*), since no code anywhere in this repo yet inserts a charge or increments `repeticiones_insertadas` — that's the cron `task 2.1`'s migration comment describes but explicitly defers, "implemented later, with the rest of §7's automation."
