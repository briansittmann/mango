## 1. Schema

- [x] 1.1 Write `supabase/migrations/0015_movimientos_recurrentes.sql` per design D8 steps 1–3: rename `gastos_fijos` to `movimientos_recurrentes`, rename its repetitions check constraint to match, add `tipo` (`gasto` | `ingreso` | `ahorro`, default `gasto`), drop `not null` on `categoria_id` and add `check (tipo <> 'gasto' or categoria_id is not null)`. Verify by applying the migration to a fresh local database and confirming an `ingreso` row inserts with a null `categoria_id` while a `gasto` row with a null `categoria_id` is rejected. **Verified by static review only** — no docker/supabase CLI/psql available in this environment to apply the migration; SQL checked by hand against 0005/0013's naming and constraint conventions (user accepted this trade-off).
- [x] 1.2 In the same migration, make `transacciones.categoria_id` nullable with the same conditional check, and rename `transacciones.gasto_fijo_id` to `movimiento_recurrente_id` along with its dependent index and unique constraint, updating the column comments. Verify the `unique (movimiento_recurrente_id, ciclo_mes)` constraint and the partial indexes still exist under their new names. **Verified by static review only**, same reason as 1.1.
- [x] 1.3 In the same migration, drop the `gastos_fijos_crud_propio` policy and create `movimientos_recurrentes_crud_propio` on the renamed table with the same `usuario_id = usuario_actual_id()` predicate. Verify row-level security is still enabled on the renamed table and the policy is listed. **Verified by static review only**, same reason as 1.1.
- [ ] 1.4 Re-run every migration from scratch in order and verify the 0012 seed still applies and the resulting schema matches the design (no `gastos_fijos` table, no `gasto_fijo_id` column). **Blocked**: requires a local Postgres/Supabase instance (docker), not available in this environment.

## 2. Data contracts

- [x] 2.1 Add `lib/data/income.ts` with `IncomeEntry`, `IncomeDraft` (aliasing `ExpenseDraft`) and `IncomeMutations` (create, update, softDelete, restore), documenting the same inputs-and-outcomes contract `ExpenseMutations` documents, with no category in any operation. Verify `npx tsc --noEmit` passes.
- [x] 2.2 In `lib/data/dashboard.ts`, replace `income.sources` with `income.entries: IncomeEntry[]`, and replace `DashboardActions.addIncome` with `income?: IncomeMutations`. Verify `npx tsc --noEmit` reports errors only at the call sites this change will update (the sample builder and the demo page/template).
- [x] 2.3 In `lib/data/recurring.ts`, add `RecurringTarget` per design D3, change `RecurringMutations.create` to take it, and give `RecurringDefinition` a `tipo` with `categoryId: string | null`, updating the contract comments. Verify `npx tsc --noEmit` flags the existing expense call site and no other. **Note:** the expense call site (`dashboard-template.tsx:532`, `recurring.create(categoryId, draft)`) is flagged as expected; `tsc` also now flags `lib/demo/demo-data.ts`, `lib/demo/demo-expenses.ts` and `lib/demo/demo-recurring.ts`, which build `RecurringDefinition` values and are fixed by part 3 (3.2–3.4), plus the income-related errors already introduced by 2.2.

## 3. Demo data source

- [x] 3.1 Add `lib/demo/demo-income.ts` with `DemoIncomeEdits` (created, updated, deletedIds), `noDemoIncomeEdits` and `createDemoIncomeMutations`, mirroring `demo-expenses.ts`. Verify `npx tsc --noEmit` passes.
- [x] 3.2 In `lib/demo/demo-data.ts`, replace the two income sources with two income entries — `Salario` 2 400 € dated day 1 of the cycle and carrying a recurring definition, `Freelance` 420 € dated 5 September — and give the base recurring definitions their `tipo`. Verify `/demo` still shows an income total of 2.820 € and a free margin of 974 €.
- [x] 3.3 In `lib/demo/demo-expenses.ts`, take the income edits in `deriveDemoData`, resolve income entries as a final step (created, updated, soft-deleted), recompute `income.total`, and compute `freeMargin` from the three resulting totals. Verify that on `/demo` adding 300 € of income moves the income column to 3.120 € and the free margin to 1.274 € while the expenses total stays 1.700 €.
- [x] 3.4 Correlate a recurring income create with the income entry created in the same submit (same name, amount and day) so one row is produced, and add the explicit `tipo === 'gasto'` filter to `attachCreatedDefinitions` and `reconcileRecurringUpdatesAndDeletes` per design D6. Verify that adding a recurring income entry produces exactly one income row and leaves the "Próximos cobros" footer at 1.025 €.
- [x] 3.5 Wire `createDemoIncomeMutations` into `app/demo/demo-dashboard.tsx` as `actions.income`, remove the `addIncome` stub, and keep `showUnavailable` for the savings add control only. Verified `npm run lint` (passes, one pre-existing unrelated warning) and the savings add row's `showUnavailable` wiring unchanged. `npx tsc --noEmit` still flags three spots in `dashboard-template.tsx` (the income panel and the recurring call site) — these are the ones 2.3's note already deferred to part 5; nothing in `lib/demo/*` or `app/demo/demo-dashboard.tsx` is flagged.

## 4. Entry sheet

- [x] 4.1 Add the income strings to `messages/es.json` and `messages/en.json` per design D5 (`hojaGasto`: `nuevoIngreso`, `editarIngreso`, `eliminarIngreso`, `ingresoAnadido`, `ingresoEliminado`, `errorEliminarIngreso`, `ingreso`, `soloEsteIngreso`; `gastoRecurrente.explicacionIngreso`), and remove the now-unused `resumen.estimado`. Verified both catalogues have identical key sets (scripted diff, empty both ways). `npx tsc --noEmit` now also flags `dashboard-template.tsx:844` (`t('estimado')`), which is the same file part 5 rewrites; no other file is newly flagged.
- [x] 4.2 In `components/organisms/entry-sheet.tsx`, widen `EntrySheetContext` to the union in design D4 so the caption renders the colour dot and category name only for a category context and the income label otherwise, using the "only this cycle's entry" wording when the income context is recurring. The `context.kind === 'category'` branch is byte-for-byte the previous caption JSX, so no visual change for expenses is possible from this edit; a live 390px/both-theme check on `/demo` is blocked right now because `dashboard-template.tsx` still calls `data.income.sources` (removed in 2.2), which throws at runtime until part 5 rewrites that panel — out of this session's requested scope (parts 3–4). `npx tsc --noEmit` confirms no new file besides `dashboard-template.tsx` is flagged.
- [x] 4.3 Export an `incomeEntry: EntryConfig<IncomeDraft>` configuration (amount, description, date, recurrence; titles, submit and delete keys from 4.1). Fields are exactly amount/description/date/recurrence, same as `expenseEntry`, with no category field descriptor — the sheet has no category control by construction, since `renderField` only ever renders what `config.fields` lists. `npx tsc --noEmit` passes for this file. Opening it live is gated on part 5 wiring it into `dashboard-template.tsx`'s `SheetTarget` (see 4.2's note).
- [x] 4.4 Make the recurrence explanation use `explicacionIngreso` when the context is income, so no category is named. `explicacionIngreso` takes only `{dia}`/`{monto}` (no `{categoria}`) in both catalogues (es: "Cada día {dia} anotaremos {monto}. Puedes cambiarlo cuando quieras."; en: "On day {dia} of every month we'll record {monto}. You can change it anytime."), and `entry-sheet.tsx` branches on `context.kind` to pick it. Live rendering is gated on part 5 (see 4.2's note).

## 5. Income panel

- [x] 5.1 In `components/templates/dashboard-template.tsx`, widen `SheetTarget` to the expense/income union and derive `config`, `context`, `initialValues`, `onSave`, `onDelete` and `onSaveRecurrence` from `target.kind`, keeping the single `EntrySheet` instance and the existing `flushSync` focus pattern. Verify creating and editing an expense still works unchanged on `/demo`. Also fixed a pre-existing bug this wiring exposed: `entry-sheet.tsx`'s delete-action label used `eliminarCargoDelMes` for any recurring context, which would have mislabelled a recurring income entry's delete button; now gated on `context.kind === 'category'`. `npx tsc --noEmit` now flags only the income panel (`dashboard-template.tsx:897-909`, `data.income.sources` / `resumen.estimado` / `actions.addIncome`), deferred to 5.2.
- [x] 5.2 Replace the income panel's `SummaryRow` list with `SwipeToDelete` + `ExpenseRow` per entry, ordered by date newest first, followed by the existing `AddRow` wired to income create. Verify each row shows name, date and amount, and that the add row's geometry still matches the expense and savings add rows at a 390px viewport. `SwipeToDelete.onDelete` is non-optional, so this step also added `openIncomeCreateSheet` and `handleDeleteIncome` (soft delete + the existing `showIncomeUndo` from 5.1) to satisfy the type — full deletion UX (single-open-row spanning expense/income, undo announcements) still verified in 5.4. Verified live on `/demo` at 390px: "Freelance" (5 sept, 420 €) lists above "Salario" (1 sept, 2.400 €), the add row's badge/label/hairline are pixel-identical to the savings panel's, free margin reads 974 €, income 2.820 €, expenses 1.700 €, no console errors. `npx tsc --noEmit` and `npm run lint` clean (one pre-existing unrelated warning).
- [x] 5.3 Wire income row activation to open the sheet in edit mode, and the add row to open it in create mode with the cycle-aware default date. Verify tap, click, Enter and Space all open edit mode, and that focus returns to the row on Escape. Added `openIncomeEditSheet` and wired `ExpenseRow.onActivate`; the create-mode default date already fell out of 5.1's shared `sheetInitialValues` branch (`clampDate(data.cycle.today, ...)`), and focus-return-on-Escape is `SheetShell`'s existing `openerRef = document.activeElement` fallback (no `finalFocusRef` needed, same as the expense rows). Verified live on `/demo`: focusing "Freelance" and pressing Enter opens "Editar ingreso" preloaded with 420 (selected), "Freelance", "5 sept 2026", and "Eliminar ingreso" (not the expense-only "cargo" wording). `npx tsc --noEmit` and `npm run lint` clean.
- [x] 5.4 Wire income deletion (swipe panel, long swipe, sheet delete) through soft delete with the existing `UndoToast`, announcing "Ingreso eliminado" and restoring on "Deshacer", and make the single-open-row rule span expense and income rows. Verify opening an income row's panel closes an open expense row's, and that undo restores the entry and every figure. No new code: 5.1 wired sheet delete (`handleSheetDelete`) and 5.2 wired `handleDeleteIncome` via `SwipeToDelete` (whose `onDelete` prop already covers both the short-swipe delete-panel path and the long-swipe auto-delete path — same component the expense rows use), and the single-open-row rule is `SwipeToDelete`'s own generic document-`pointerdown` listener, which was never expense-specific. Verified live on `/demo` via simulated pointer drags: swiping "Freelance" open reveals the trash button, tapping it shows a toast reading "Ingreso eliminado" with "Deshacer", income drops to 2.400 € and the free margin to 554 €; "Deshacer" restores 2.820 €/974 €. Separately, swiping "Alquiler" open (offset -68px) and then swiping "Salario" open closed the expense row (offset back to 0) while the income row opened, confirming the shared rule. `npx tsc --noEmit` and `npm run lint` clean.
- [x] 5.5 Disable the income rows and add row when `actions.income` is absent, showing amounts as plain text without the editable-looking background. Verify by mounting the dashboard without income operations that nothing opens and no row moves when dragged. No new code: the `actions.income ?` conditionals already added in 5.2/5.3 (row `onActivate`, `SwipeToDelete` vs. plain `div`, `AddRow`'s own `onClick`/`disabled`) already implement this — the same generic mechanism the expense rows use. Verified live: temporarily dropped `income` from `app/demo/demo-dashboard.tsx`'s `actions` object (no repo has a fixture for mounting without handlers, so this mirrors how the pre-existing expense case would be checked), confirmed "Añadir ingreso" renders `[disabled]`, "Salario"/"Freelance" are no longer buttons (plain text, no pill background), a swipe gesture finds no `SwipeToDelete` wrapper and the row doesn't move, then reverted the edit (`git diff` confirms `income` is back).

## 6. Tests and verification

- [x] 6.1 Add a Playwright spec covering create: adding an income entry on `/demo` moves the income column to 3.120 € and the free margin to 1.274 €, leaves the expenses total at 1.700 €, and announces "Ingreso añadido". Verify with `npm test`. Added `tests/income-create.spec.js`, following `recurring-create.spec.js`'s pattern (aria-hidden-stripped `body()` polling for the rolling-counter totals). `npx playwright test tests/income-create.spec.js --project=chromium` passes.
- [x] 6.2 Add a Playwright spec covering edit and delete-with-undo: editing "Salario" to 2 500 gives 2.920 € and 1.074 €; deleting "Freelance" gives 2.400 € and 554 €, and "Deshacer" restores 2.820 € and 974 €. Verify with `npm test`. Added `tests/income-edit-delete.spec.js` (edit via the sheet, delete via the sheet's "Eliminar ingreso" + undo toast). `npx playwright test tests/income-edit-delete.spec.js --project=chromium` passes (2/2).
- [x] 6.3 Add a Playwright spec covering recurring income: the switch, day 5 and a repetitions count produce one income row, no new row in "Próximos cobros", a footer still at 1.025 € and a collapsed next charge still "Parking · día 15". Verify with `npm test`. Added `tests/income-recurring.spec.js`. Writing it surfaced a genuine correctness bug in 3.4's implementation: `deriveDemoData` (`lib/demo/demo-expenses.ts`) called `attachCreatedIncomeDefinition` on `base.income.entries` (the untouched sample) and only afterward called `resolveIncome` to add this submit's newly created entry — so the freshly created plain row was never in the list the correlation searched, and `resolveIncome` then added it a second time, producing two rows instead of one. Fixed by resolving income edits first, then correlating the recurring definition against the resolved list, mirroring the expense pipeline's step 2-before-step-3 order. `npx tsc --noEmit` clean; `npx playwright test tests/income-recurring.spec.js --project=chromium` passes.
- [x] 6.4 Add coverage for the reduced-motion and both-theme paths of the income rows and add row (reusing the approach in `tests/recurring-motion-a11y.spec.js`): no badge growth or plus rotation under `prefers-reduced-motion`, and label contrast met in light and dark. Verify with `npm test`. Added `tests/income-motion-a11y.spec.js` (badge/plus `transform` unchanged after hover under `prefers-reduced-motion: reduce`; `CONTRAST_CHECK` on `#summary-group-panel` in both themes) — the income rows and add row are the same `ExpenseRow`/`SwipeToDelete`/`AddRow` components the expense and savings panels already use, so this exercises the shared motion/contrast behavior specifically inside the income panel's DOM. `npx playwright test tests/income-motion-a11y.spec.js --project=chromium` passes (3/3).
- [x] 6.5 Run `npm run lint`, `npx tsc --noEmit`, `npm run build` and `npm test`, and confirm the existing specs (`recurring-create`, `recurring-scope`, `recurring-motion-a11y`, `reorder-mode`, `category-sheet-header`, `animated-amount`, `example`) still pass with their current figures. `npm run lint` (1 pre-existing unrelated warning in `lib/whatsapp/adapter.ts`), `npx tsc --noEmit` and `npm run build` are all clean. `npm test` (chromium/firefox/webkit): found a pre-existing, unrelated failure predating this session — `tests/reorder-mode.spec.js` hardcoded "Comida, posición 2/3 de 7", but the category order committed at HEAD (`vivienda, salud, hogar, comida, ocio, transporte, compras`) puts Comida at position 4/5. Confirmed via `git show HEAD` that this order was already committed before this session and is untouched by add-income-management. Per user direction, fixed the test's expectation (positions 2→4, 3→5) rather than leaving it broken. Also found and confirmed (not fixed, out of scope) an intermittent webkit-only focus-transfer flake in the same "moves a card with the keyboard" test (1/30 runs across chromium+firefox+webkit ×2), unrelated to the position numbers — chromium and firefox are fully stable. Two more failures seen in a full parallel `npm test` run (chromium `animated-amount`, firefox `recurring-motion-a11y`) are known load-sensitive flakes per those files' own comments about frame-timing under parallel load; both pass in isolation. All four new income spec files pass on all three browsers.
- [x] 6.6 Verify the delivered UI by hand on `/demo` at a 390px viewport in both themes and both languages: open the income panel, add, edit, swipe-delete and undo an entry, add a recurring one, and confirm the income panel's rows, sheet and toast match the expense equivalents and that no "estimado" reference remains. `grep`-confirmed no `estimado`/`.sources` reference remains anywhere in code or messages. Verified live (light + Spanish, via simulated pointer drags and keyboard, screenshots) in 5.2-5.5: row geometry, tap/Enter/Escape activation and focus return, swipe-reveal + long-swipe delete with "Ingreso eliminado"/"Deshacer" toast restoring every figure, single-open-row spanning expense/income, and the disabled state with no handler. Verified live (dark theme + English, screenshots) here: the income panel ("Freelance"/"Salary" rows, "Add income"), the recurring "Salary" entry's edit sheet ("This month's income only" caption, "Delete income" — not the expense "cargo" wording), and the create sheet ("New income", "Income" caption, no category field, "Repeats every month" toggle) — all match the expense sheet's structure and English strings correctly.
