## 1. Schema

- [x] 1.1 Add `supabase/migrations/0016_usuarios_meta_ahorro.sql`: one nullable `meta_ahorro_mensual numeric(12,2)` on `usuarios` with `check (meta_ahorro_mensual is null or meta_ahorro_mensual > 0)`, plus a `comment on column` saying it is the per-cycle savings target, that null means the user has not set one, and that it lives here rather than in `presupuestos` (proposal.md §A). Verify by reading the file back against the four reasons in §A — no table created, nothing altered, no existing constraint touched.
- [x] 1.2 Add `meta_ahorro_mensual` to the `usuarios` field list in ARCHITECTURE.md §8, and rewrite §9's savings bullet so "progreso hacia la meta" describes what exists rather than what is planned. Verify by grepping both sections for the column name and for the old aspirational wording.
- [x] 1.3 Record in the change's own notes that 0016 is **not** verified against a live database — there is no docker, Supabase CLI or psql in this environment, the same limitation `add-income-management` hit at its task 1.4. Verify by confirming the limitation is written down where the archive step will copy it into CLAUDE.md's Deuda técnica, not silently dropped.
  - **Verified by static review only.** `0016_usuarios_meta_ahorro.sql` was not applied to a live database — no docker/Supabase CLI/psql in this environment, same limitation `add-income-management` recorded at its task 1.4. Checked by hand against 0008/0014's `comment on column` and `check` conventions. Carry into CLAUDE.md's Deuda técnica at archive (7.5), alongside the existing 0015 entry.

## 2. The pure function

- [x] 2.1 Create `lib/data/savings.ts` with the `SavingsProgress` type and `getSavingsProgress({ target, movements })` exactly as typed in proposal.md §B: `target` non-nullable in the signature, raw `deposited`/`withdrawn`/`net` unclamped, `netRatio` and `depositedRatio` floored at 0 and capped at 1, `reached` from the raw net. Verify with `npx tsc --noEmit`.
- [x] 2.2 Add `target: number | null` and `history: { month: string; accumulated: number }[]` to `DashboardData['savings']` in `lib/data/dashboard.ts`, each with a comment naming its source column or query. Verify with `npx tsc --noEmit` — every construction site of `DashboardData` must fail until it supplies both.
- [x] 2.3 Write `tests/savings-progress.spec.js` covering `getSavingsProgress` as plain assertions with no page: **no movements** (net 0, ratios 0, not reached), **with withdrawals** (176/−80/50 against 300 → deposited 226, withdrawn 80, net 146, `netRatio` ≈ 0.4867, `depositedRatio` ≈ 0.7533, not reached), **target exceeded** (net 312 against 300 → `reached` true, both ratios capped at 1, `net` still 312), and **negative net** (withdrawals over deposits → `net` negative, `netRatio` 0, not reached). Verify with `npm test -- savings-progress`; if the `@/` alias does not resolve inside a Playwright spec, import by relative path rather than adding a runner (see 2.4).
- [x] 2.4 Confirm the unit cases run inside the existing Playwright project without a new dependency, and that `package.json` gains no devDependency. Verify with `git diff package.json` returning nothing and `npm test` running both the function cases and the browser specs in one command.

## 3. Demo data

- [x] 3.1 In `lib/demo/demo-data.ts`, set `savings.target` to 300 and build `savings.history` as six points ending at the accumulated balance (2.646), one per month of the existing `history` array so both charts cover the same cycles. Verify that the last history point equals `savings.accumulated` by assertion in the spec from task 6.3, not by eye.
- [x] 3.2 Carry `target` and `history` through `deriveDemoData` in `lib/demo/demo-expenses.ts` untouched — it already spreads `base`, so confirm rather than add, and confirm no savings figure is recomputed from expense edits. Verify by loading `/demo`, editing an expense, and checking the savings total, the accumulated balance and the bar are unchanged.
- [x] 3.3 Verify no existing figure moved: `/demo` in Spanish still shows the expenses total 1.700 €, the free margin 974 €, the savings total 146 € and the accumulated 2.646 €. Verify by running the existing suite — `npm test` — with no spec edited for this task.

## 4. The bar

- [x] 4.1 Extend `components/atoms/progress-bar.tsx` with an optional second, hatched segment: a `hatchedTo` fraction drawn after the solid fill as a 45° `repeating-linear-gradient` in `--muted-foreground` at low alpha, inside the same rounded track. The budget bar passes nothing and its rendered output must be byte-identical to today's. Verify by opening the "comida" card before and after and diffing the computed styles of its fill.
- [x] 4.2 Add `components/molecules/savings-progress.tsx`: the track from 4.1 at 6px with `--brand` fill, the `reached` check mark at the end of the track, and the caption under it from a new `resumen` key rendering "{ahorrado} de {meta}" in the muted colour with no currency symbol. No colour literal, no `--warning`, no `--destructive`. Verify by grepping the file for `#`, `rgb`, `warning` and `destructive` and finding nothing.
- [x] 4.3 Give the track `role="progressbar"`, `aria-valuenow` from `netRatio` and an `aria-valuetext` naming both figures **with** currency, from its own message key. Verify with the accessible-name assertion in the spec from task 6.2.
- [x] 4.4 Add an optional `below` slot to a tile in `components/organisms/summary-group.tsx`, rendered under the total inside the same button, and pass the savings progress into it from `dashboard-template.tsx` only when `data.savings.target != null`. The other two tiles pass nothing. Verify that the three columns still compute the same height and that the savings column is the only one with content below its total.
- [x] 4.5 Check the caption against `design-system`'s six-font-size ceiling: it must reuse an existing metadata size, since it is visible with everything collapsed. Verify by re-running the type-scale assertion at a 390px viewport with the bar present.

## 5. The panel

- [x] 5.1 Add `components/molecules/savings-movement-row.tsx`: a 28px circle on `--muted` holding an up or down arrow, the name with its `ShortDate` under it, and the amount composed as sign + `Money` with `Math.abs(amount)` — `+` in `--brand-ink` for a deposit, U+2212 in `--foreground` for a withdrawal, no space between sign and digits, `signDisplay` unused. Verify by asserting in the spec from task 6.2 that the withdrawal's text contains U+2212 and not U+002D, and that the sign is not `--destructive`.
- [x] 5.2 Confirm the row still clears 48px with the circle in it, and that the circle is not focusable and not a nested surface under `design-system`'s definition (border + ≥12px radius + ≥64px height). Verify by tabbing through the open panel and by measuring the row.
- [x] 5.3 Add `components/atoms/savings-sparkline.tsx`: Recharts, ~80 × 24px, a `--brand` stroke, `dot={false}`, no axes, no grid, no tooltip, no legend, hidden from assistive technology. Verify that the panel's accessible tree announces the accumulated amount exactly once.
- [x] 5.4 Rebuild the accumulated block in `dashboard-template.tsx` as its own element rather than a `SummaryRow`: muted label, sparkline between label and amount, regular-weight amount with `tabular-nums`, extra bottom spacing before the movement list, no `onClick`, no chevron, not focusable. Verify with the three accumulated-block scenarios in the `dashboard-ui` delta.
- [x] 5.5 Replace the savings `SummaryRow`s with `SavingsMovementRow` in `dashboard-template.tsx`, leaving the `AddRow` and `actions.addSavingsMovement` exactly as they are. Verify that "Añadir movimiento de ahorro" still shows the demo toast and changes nothing.
- [x] 5.6 Check every amount in the panel — accumulated and movements — computes `font-variant-numeric: tabular-nums` and shares one right edge. Verify with the alignment scenario in the `dashboard-ui` delta.

## 6. Motion, i18n and tests

- [x] 6.1 Wire the motion: the bar fills from 0 on first reveal through the atom's existing `IntersectionObserver` gate at ~600ms on `--ease-out` and does not replay on later panel openings; the movement rows enter on a gsap timeline with a 40ms stagger inside the `Collapsible`'s height animation; both read `prefers-reduced-motion` once and `gsap.set(..., { clearProps })` to the final state, the way `AnimatedContent` does. No new dependency. Verify with `git diff package.json` empty and the motion scenarios in the `design-system` delta.
- [x] 6.2 Add to `tests/savings-progress.spec.js` the rendered cases on `/demo` at a 390px viewport: open the savings card, assert the 6px track exists under the savings total, that the solid fill spans 48–49 % and the hatch continues to 75–76 %, that the caption reads "146 de 300", that the withdrawal uses U+2212 in the regular colour, and that the fill's computed colour equals `--brand` and equals neither `--warning` nor `--destructive`. Verify with `npm test -- savings-progress`.
- [x] 6.3 Add the reduced-motion cases to the same spec: with `prefers-reduced-motion: reduce` emulated, the fill is at its final width in the first frame and every movement row is at full opacity and in place in the first frame after the panel opens. Verify with `npm test -- savings-progress`.
- [x] 6.4 Add the new keys to `messages/es.json` and `messages/en.json` under `resumen`: the caption, the bar's `aria-valuetext`, the check's accessible name, the sparkline's description if it is not hidden, and the deposit/withdrawal icon labels — reusing the existing `resumen.deposito` and `resumen.retiro` rather than adding near-duplicates. Verify that both files parse, have identical key sets, and that no new string is hardcoded in a component (grep the new files for quoted Spanish).
- [x] 6.5 Check the card in both themes and both languages at 390px: contrast of the caption against the card, of `--brand-ink` on a deposit, and of the hatch against the track; the English caption not wrapping in a 122px column. Verify with screenshots of all four combinations.

## 7. Verification

- [x] 7.1 Run `npx tsc --noEmit` and `npm run lint` clean.
- [x] 7.2 Run the whole suite with `npm test` and confirm every pre-existing spec passes untouched — `animated-amount.spec.js` in particular, which addresses the savings tile by `[aria-controls="summary-group-panel"]:nth-child(3)` and must keep finding it after the tile gains content below its total.
- [x] 7.3 Render `/demo` with `savings.target` temporarily set to `null` and confirm the card is byte-for-byte today's card: no track, no caption, no check, no placeholder gap, and the column not exposed as disabled. Restore 300 afterwards. Verify against the "No target, no bar" scenario.
- [x] 7.4 Run `openspec validate add-savings-progress --strict` and fix anything it reports.
- [x] 7.5 Update CLAUDE.md's Deuda técnica: narrow the "UI de ahorro" entry to say the panel is styled but still has no `SavingsMutations`, and add the two items this change defers — the target has no editing UI anywhere, and 0016 was never run against a real database (1.3). Verify by reading the section back against what was actually built.
