## 1. Groundwork

- [ ] 1.1 Confirm with the user that the untracked v0 files under `components/` are committed before anything is edited, so `finance-dashboard.tsx` stays recoverable. Verify: `git log --oneline -- components/finance-dashboard.tsx` prints a commit.
- [ ] 1.2 Install `next-intl`, `lucide-react`, `recharts`, `react-is@19`, `@base-ui/react`, `class-variance-authority`, `clsx`, `tailwind-merge`, and add `lib/utils.ts` exporting `cn` (design D12). Verify: `npm ls` shows no unmet peer dependencies, and `npx tsc --noEmit` reports no errors in `components/ui/button.tsx`.
- [ ] 1.3 Add the D4 lint rules to `eslint.config.mjs` (`@typescript-eslint/no-restricted-imports` with `allowTypeImports` on `components/**`; `react/jsx-no-literals` on `components/dashboard/**`). Verify: a throwaway `components/dashboard/_probe.tsx` that imports `buscarUsuarioIdPorTelefono` and renders literal text makes `npm run lint` fail on both rules. `import type { SupabaseClient } from '@supabase/supabase-js'` in the same file does not trip the import rule. Delete the probe afterwards.

## 2. Theme tokens

- [ ] 2.1 Replace `app/globals.css` with the D7 structure (`color-scheme` switch, every token defined once with `light-dark()`, `@theme inline` mapping with shadcn names + `brand`, `brand-ink`, `warning`, `hero-glow`, `--cat-<nombre>` for the 8 stored colour names), using the D8 values and keeping the Geist font variables. Verify: `npm run build` succeeds, and `grep -rnE "#[0-9A-Fa-f]{6}" app components lib` matches only `app/globals.css`.
- [ ] 2.2 Add the no-flash theme script to `<head>` in `app/layout.tsx` with `<html suppressHydrationWarning>`, plus a client `components/theme/theme-sync.tsx` that re-applies `data-theme` in `useLayoutEffect` (design D7). Verify in the browser on `/`: with OS emulated dark and `localStorage.theme = 'light'`, a hard reload gives a light `body` background from first paint with no hydration error in the console. With the key removed, toggling the emulated OS preference switches the background live.
- [ ] 2.3 Contrast pass on the provisional D8 values. A scratchpad script checks `foreground`, `muted-foreground` and `brand-ink` against `card` and `background` in both themes (≥ 4.5:1), and every `--cat-*` against `card` (target ≥ 3:1; any exception must stay visible thanks to the pie separator). Adjust `globals.css` and update the D8 table in `design.md`. Verify: the script's output table shows every text pair passing.

## 3. Localization plumbing

- [ ] 3.1 Add `i18n/request.ts` (cookie `locale`, only `es|en`, default `es`, `timeZone: 'UTC'` fallback), wrap `next.config.ts` with `createNextIntlPlugin()`, and set `<html lang={locale}>` + `<NextIntlClientProvider>` in `app/layout.tsx`. Verify with `npm run dev`: `curl -s localhost:3000 | grep -o '<html lang="[a-z]*"'` prints `es`. With `-H 'Cookie: locale=en'` it prints `en`, and with `-H 'Cookie: locale=fr'` it prints `es`.
- [ ] 3.2 Create `messages/es.json` and `messages/en.json` with the namespaces from D9, `global.d.ts` augmenting next-intl `AppConfig.Messages` with `typeof es`, and `messages/paridad.ts` asserting both directions. Verify: `npx tsc --noEmit` passes. Temporarily deleting one key from `en.json` makes it fail (restore afterwards).
- [ ] 3.3 Add `i18n/formatos.ts` with the D9 money options (`trailingZeroDisplay: 'stripIfInteger'`, `useGrouping: 'always'`) and compact-number options. Verify: `node -e "import('./i18n/formatos.ts').then(…)"` formats 2400, 62.4 and 820 as `2.400 €`, `62,40 €` and `820 €` in `es`, and 2400 as `€2,400` in `en`.
- [ ] 3.4 Add the `cambiarIdioma` server action in `app/acciones/idioma.ts` (validates `es|en`, sets `locale` cookie `path=/`, 1 year, `SameSite=Lax`). Verify: `npx tsc --noEmit` passes, and calling it with `'fr'` leaves the cookie unchanged (checked in 5.4 through the UI).

## 4. Data contract, demo data and route

- [ ] 4.1 Write the D2 types in `lib/datos/dashboard.ts` (`ColorCategoria` matching the `0004_categorias.sql` check, `DatosDashboard`, `GrupoGasto`, `Acciones`). Verify: `npx tsc --noEmit` passes, and the `ColorCategoria` members equal the eight names in the migration's `check` list.
- [ ] 4.2 Implement `estadoPresupuesto` in `lib/datos/presupuesto.ts` (D3: today counts, floor, `restante ≥ 0`, weekly `null` under 7 days, levels at 0.8 / 1.0). Verify with a Node one-liner importing the file: `{monto:400,gastado:310,diaActual:10,diasCiclo:30}` → `disponibleSemanal 30`, `nivel 'ok'`. `{120, 98, 27, 30}` → `diasRestantes 4`, `disponibleSemanal null`, `restante 22`, `nivel 'alerta'`. `{100, 130, 10, 30}` → `restante 0`, `nivel 'excedido'`.
- [ ] 4.3 Build `datosDemo(locale)` in `lib/demo/datos-demo.ts` (D10). Fictional rows with `{es, en}` names; cycle 1–30 September 2026 with reference day 10 and `enCurso: true`; a fijos group plus ≥ 6 categories using at least six colour names; `comida` budget 400 with 310 spent, one category in *alerta* and one *excedido*; income sources with estimated vs real; savings deposits and one withdrawal; 6 cycles of history; amounts 2400, 62.40 and 820 present; one expense at `2026-09-02T23:30:00Z` with timezone `Europe/Dublin`. Every total is derived from rows via `estadoPresupuesto` and sums. Verify: `npx tsc --noEmit` passes, and `grep -n "psicól\|préstamo\|+353" lib/demo/datos-demo.ts` finds nothing.
- [ ] 4.4 Create `app/demo/page.tsx` rendering `components/dashboard/dashboard.tsx` (initially just the shell and `demo-notice.tsx`) with `datosDemo(await getLocale())` and `acciones={{ cambiarIdioma }}`. Verify: `curl -s localhost:3000/demo` returns 200 with the Spanish sample-data notice. With `Cookie: locale=en` the English notice appears, and no login redirect happens.

## 5. Dashboard components (verify each on `/demo` with the browser-automation skill, Spanish + both themes)

- [ ] 5.1 `category-card.tsx`, `expense-row.tsx`, `add-row.tsx` and `budget-progress.tsx`, wired into `dashboard.tsx` with the D6 `Set` of open ids and "Colapsar todo". Verify: the fijos card comes first, then one card per category, all the same component, all collapsed on load. Two cards can be open at once, and "Colapsar todo" closes both. `comida` shows "310 de 400" and "30 por semana". The *alerta* and *excedido* bars use warning and destructive. The expanded border is not the category colour. Duplicate-named rows both render, and "Añadir gasto" is disabled.
- [ ] 5.2 `summary-card.tsx`, `summary-row.tsx` and the income / expenses / savings panels. Verify: opening savings closes income; the expenses panel is sorted by total descending and its "Ver todos los gastos" scrolls to the cards; income rows show real with the estimate in muted text; the savings withdrawal is visually distinct; the expenses summary total equals the sum of card totals.
- [ ] 5.3 `month-selector.tsx`, `free-margin-card.tsx` and `account-avatar.tsx`. Verify: the title reads "Septiembre" with "en curso" and both chevrons disabled; the hero is the largest number on the page with a glow in dark and none in light; the avatar is 36px (computed size), shows the name's initial, and its computed colours don't use `--brand`.
- [ ] 5.4 `account-menu.tsx` with name, phone, theme control (light / dark / automatic) and language control calling `acciones.cambiarIdioma` then `router.refresh()`. Verify: no e-mail and no plan badge; every row's computed height is ≥ 48px and font-size ≥ 12px. Choosing English switches all text, keeps the URL at `/demo`, sets `<html lang="en">` and survives a reload. Choosing light survives a reload on a dark-emulated OS. Automatic follows emulated OS changes live.
- [ ] 5.5 `category-pie-chart.tsx` and `monthly-bars-chart.tsx` with Recharts (D11). Verify: the number of slices equals the number of non-empty cards, and slice fills match the card dots; a separator is visible between slices (including `gris_oscuro` in dark); no legend element exists; the current cycle bar uses brand; axis labels are Spanish short months and compact figures (`1,8 mil`), switching to English (`1.8K`) after changing language. If colours don't resolve through the `fill` attribute, switch to `style` (design risk).
- [ ] 5.6 Delete `components/finance-dashboard.tsx`. Verify: `grep -rn "finance-dashboard\|lib/translations" app components lib` returns nothing and `npm run build` succeeds.

## 6. Cross-cutting verification

- [ ] 6.1 Guards: `grep -rnE "#[0-9a-fA-F]{3,8}\b|rgba?\(|hsla?\(|-(white|black)(/|\b)" components/dashboard` and `grep -rn 'aria-label="' components/dashboard` both return nothing, and `npm run lint` passes.
- [ ] 6.2 Data isolation: on `/demo`, open every card, every summary panel and the account menu while recording network traffic. Verify no request goes to the host in `NEXT_PUBLIC_SUPABASE_URL`.
- [ ] 6.3 Walk through the scenarios in `specs/` on `/demo` in all four combinations (es/en × light/dark) and save screenshots. Verify: no console errors or hydration warnings; `2.400 €` / `62,40 €` / `820 €` in Spanish and `€2,400` in English; the `2026-09-02T23:30Z` expense shows 3 Sep; the `blanco` category dot is visible in light; category names switch with the demo locale.
- [ ] 6.4 Final gate: `npx tsc --noEmit`, `npm run lint` and `npm run build` all pass, and the build output lists `/demo`. Run `openspec validate land-finance-dashboard` and confirm it passes.
